'use client';

/**
 * Push nativo (FCM) vive en la app Expo (`mobile-app`).
 * En la web no registramos tokens Capacitor; si más adelante querés web push, sería otro flujo (service worker).
 */
export default function PushRegistration() {
  return null;
}
