# Documentación en este directorio

- **Este repo (raíz):** proyecto **Next.js** (web + rutas `/api/*`). Lo que queda aquí son notas de admin, billing, edge functions, seguridad, diagnósticos puntuales, etc.
- **App móvil (APK / iOS):** todo el cliente nativo está en **`mobile-app/`**. Empezá por `mobile-app/README.md` y `mobile-app/docs/BUILD_ANDROID.md`.

Se eliminaron guías que hablaban de **Capacitor** o de un `android/` en la raíz; ese enfoque ya no se usa.
