# OBSOLETO (evitar confusión entre build local y cloud).
# La forma oficial actual para generar APK release local es:
#   cd mobile-app
#   npm run build:android:release

Write-Host "OBSOLETO: usa `npm run build:android:release` desde `mobile-app`." -ForegroundColor Yellow
exit 1
