/**
 * Paradas intermedias que el conductor puede marcar al publicar (mapa → ride_stops).
 * Un solo valor para pantalla preview, modal y validación al tocar el mapa.
 */
export const MAX_DRIVER_PUBLISH_WAYPOINTS = 10;
